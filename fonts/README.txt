Boxicons字体使用说明
====================

为解决Boxicons字体加载慢的问题，您需要手动下载字体文件并放在此文件夹中：

1. 访问 https://cdn.jsdelivr.net/npm/boxicons@latest/fonts/boxicons.woff2 下载最新的boxicons.woff2文件
2. 将下载的boxicons.woff2文件放在此fonts目录中
3. 系统已经配置为使用本地字体文件，不再从CDN加载

这样可以避免浏览器显示"Slow network is detected"的警告消息。 